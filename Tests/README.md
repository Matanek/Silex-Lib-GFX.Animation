# GFX.Animation tests

`Timeline.sx` validates timeline composition and its GFX integrations.
`Consumer` is an anonymous application workspace executed from its own
directory to verify the public package and plugin-catalog boundary.
